{
  "name": "sat-trainer",
  "version": "1.0.0",
  "private": true,
  "description": "A zero-dependency static Digital SAT trainer (the app itself needs no build/install). jsdom is a dev-only test dependency.",
  "scripts": {
    "test": "node test/smoke.js",
    "serve": "python3 -m http.server 8123"
  },
  "dependencies": {
    "jsdom": "^29.1.1"
  }
}
